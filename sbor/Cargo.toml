[package]
name = "sbor"
version = "0.1.0"
edition = "2021"

[dependencies]
hex = { version = "0.4.3", default-features = false }
phf = { version = "0.11", default-features = false, features = ["macros"] }